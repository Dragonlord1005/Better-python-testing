{
  "extends": [
    "config:base"
  ],
  "semanticCommits": "enabled",
  "baseBranches": ["main", "TOCD", "Inventory"],
  "packageRules": [
    {
      "matchDepTypes": ["dependencies"],
      "matchUpdateTypes": ["patch", "minor"],
      "groupName": "dependencies (non-major)"
    }
  ],
}
