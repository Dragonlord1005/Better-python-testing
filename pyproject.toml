[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]

[tool]
[tool.poetry]
authors = ["Your Name <you@example.com>"]
description = ""
name = "better-python-testing"
version = "0.1.0"
[tool.poetry.dependencies]
python = "^3.8"
pygame = "^2.0.1"
pygame-menu = "^4.0.2"
pygame-gui = "^0.5.7"
pip = "^21.0.1"
[tool.poetry.dev-dependencies]
